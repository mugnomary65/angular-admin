import { FilerServiciosPipe } from './filer-servicios.pipe';

describe('FilerServiciosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilerServiciosPipe();
    expect(pipe).toBeTruthy();
  });
});
